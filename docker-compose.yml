version: '3.7'
services:
  socketio:
    build: .
    command: npm start
    restart: always
    ports:
      - 3009:${SERVER_PORT}
    env_file:
      - .env
    volumes:
      - "./data:/usr/src/app/data"